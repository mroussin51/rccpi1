# RCCPI AEO CSS Patch

This patch makes all AEO pages use your existing `/style.css` and removes their inline styles.

## Install
```bash
# From your site root (e.g., ~/docker/rccpi.net/site)
unzip ~/Downloads/rccpi_aeo_css_patch.zip -d ./aeo
# (use -o to overwrite if prompted)
```
